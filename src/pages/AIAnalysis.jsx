import React, { useState, useCallback } from 'react';&#10;import '../styles/ai-analysis.css';&#10;import { analyzeMedia } from '../services/aiAnalysis.service.js';&#10;&#10;const AIAnalysis = () => {&#10;  const [selectedFile, setSelectedFile] = useState(null);&#10;  const [preview, setPreview] = useState('');&#10;  const [loading, setLoading] = useState(false);&#10;  const [result, setResult] = useState(null);&#10;  const [error, setError] = useState('');&#10;&#10;  const handleFileSelect = useCallback((file) => {&#10;    if (file &amp;&amp; (file.type.startsWith('image/') || file.type.startsWith('video/'))) {&#10;      setSelectedFile(file);&#10;      const previewUrl = URL.createObjectURL(file);&#10;      setPreview(previewUrl);&#10;      setResult(null);&#10;      setError('');&#10;    } else {&#10;      setError('Please select an image or video file.');&#10;    }&#10;  }, []);&#10;&#10;  const handleAnalyze = async () => {&#10;    if (!selectedFile) return;&#10;&#10;    setLoading(true);&#10;    setError('');&#10;    try {&#10;      const analysisResult = await analyzeMedia(selectedFile);&#10;      setResult(analysisResult);&#10;    } catch (err) {&#10;      setError('Analysis failed. Please try again. Backend API may not be available.');&#10;    } finally {&#10;      setLoading(false);&#10;    }&#10;  };&#10;&#10;  const handleDrop = useCallback((e) => {&#10;    e.preventDefault();&#10;    const file = e.dataTransfer.files[0];&#10;    handleFileSelect(file);&#10;  }, [handleFileSelect]);&#10;&#10;  return (&#10;    <div className="ai-analysis-page">&#10;      <div className="ai-analysis-container">&#10;        <h1>AI Media Analysis</h1>&#10;        <div &#10;          className={`upload-area ${selectedFile ? 'has-file' : ''}`}&#10;          onDrop={handleDrop}&#10;          onDragOver={(e) => e.preventDefault()}&#10;        >&#10;          <input&#10;            type="file"&#10;            accept="image/*,video/*"&#10;            onChange={(e) => handleFileSelect(e.target.files[0])}&#10;            id="media-upload"&#10;            style={{display: 'none'}}&#10;          />&#10;          <label htmlFor="media-upload" className="upload-button">&#10;            {selectedFile ? 'Change File' : '📁 Choose Image or Video'}&#10;            <p>or drag &amp; drop</p>&#10;          </label>&#10;          {selectedFile &amp;&amp; preview &amp;&amp; (&#10;            <div className="preview-container">&#10;              {selectedFile.type.startsWith('image/') ? (&#10;                <img src={preview} alt="Preview" className="media-preview" />&#10;              ) : (&#10;                <video src={preview} controls className="media-preview">&#10;                  Your browser does not support video preview.&#10;                </video>&#10;              )}&#10;              <p>{selectedFile.name}</p>&#10;            </div>&#10;          )}&#10;        </div>&#10;&#10;        {selectedFile &amp;&amp; !loading &amp;&amp; !result &amp;&amp; (&#10;          <button onClick={handleAnalyze} className="analyze-button">&#10;            🔍 Analyze with AI&#10;          </button>&#10;        )}&#10;&#10;        {loading &amp;&amp; (&#10;          <div className="loading-state">&#10;            <div className="spinner"></div>&#10;            <p>Processing your media with AI...</p>&#10;          </div>&#10;        )}&#10;&#10;        {error &amp;&amp; (&#10;          <div className="error-box">❌ {error}</div>&#10;        )}&#10;&#10;        {result &amp;&amp; (&#10;          <div className="result-card">&#10;            <h2>Analysis Result</h2>&#10;            <div className="result-type">🚨 Type: <strong>{result.type}</strong></div>&#10;            <div className="confidence-bar">&#10;              Confidence: <strong>{Math.round(result.confidence * 100)}%</strong>&#10;              <div className="confidence-fill" style={{width: `${result.confidence * 100}%`}} />&#10;            </div>&#10;            <div className="result-description">{result.description}</div>&#10;            {result.objects &amp;&amp; result.objects.length > 0 &amp;&amp; (&#10;              <div className="detected-objects" >&#10;                Detected: <span className="objects-list">{result.objects.join(', ')}</span>&#10;              </div>&#10;            )}&#10;          </div>&#10;        )}&#10;      </div>&#10;    </div>&#10;  );&#10;};&#10;&#10;export default AIAnalysis;
